import type { Metadata } from "next";
import { Geist, Geist_Mono, Great_Vibes, Cormorant_Infant } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

const greatVibes = Great_Vibes({
  weight: "400",
  variable: "--font-great-vibes",
  subsets: ["latin", "cyrillic"],
})

const cormorantInfant = Cormorant_Infant({
    weight: "400",
    variable: "--font-cormorant-infant",
    subsets: ["latin", "cyrillic"],
 });

export const metadata: Metadata = {
  title: "AV Wedding",
  description: "Generated by love",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable} ${greatVibes.variable} ${cormorantInfant.variable}`}>
        {children}
      </body>
    </html>
  );
}
